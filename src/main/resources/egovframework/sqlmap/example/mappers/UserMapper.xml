<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="egovframework.example.sample.service.UserMapper">

	<select id="getAllUsers" resultType="egovframework.example.sample.service.UserVO">
	    SELECT * FROM user
	</select>

    <insert id="insertUser" parameterType="egovframework.example.sample.service.UserVO">
        INSERT INTO user (username, password)
        VALUES (#{username}, #{password})
    </insert>

</mapper>
